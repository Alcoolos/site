{% extends 'admin.html.twig' %}

{% block css %}
  {{ parent() }}
  <link rel="stylesheet" type="text/css" href="/css/article.css" />
{% endblock %}

{% block main %}

<div class="grid grid-pad">
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}

    <a href="{{app.request.headers.get('referer')}}"><button>Back</button></a>
</div>
{% endblock %}
